import { PropTypes } from 'react';

export default {
  category: PropTypes.string,
  viewStats: PropTypes.func,
  showDailyStats: PropTypes.func,
  showWeeklyStats: PropTypes.func,
  showMonthlyStats: PropTypes.func,
};
